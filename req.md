# Maple Chat 개발 명세서

> 상태: 심층 인터뷰 확정본  
> 작성 기준일: 2026-08-04 (Asia/Seoul)  
> D-001~D-017과 본문 요구사항은 구현 계약이다. 외부 권리 승인 게이트는 코드 완료와 별도로 충족해야 한다.

## 1. 제품 개요

메이플스토리 인벤 게시물과 질의응답성 댓글을 검색 근거로 사용하는 단일 Discord 서버용 한국어 RAG 챗봇을 만든다. 사용자는 봇을 멘션하여 자연어로 질문하고, 봇은 관련 커뮤니티 자료를 검색한 뒤 로컬 LLM으로 답변한다.

## 2. 인터뷰 결정 기록

| ID | 항목 | 결정 | 상태 |
|---|---|---|---|
| D-001 | 주 사용 방식 | Discord에서 봇을 멘션한 자연어 Q&A | 확정 |
| D-002 | 초기 배포 범위 | 개인 Discord 서버 1곳 | 확정 |
| D-003 | 출처 표시 | 기본 응답에는 생략하고 사용자가 요청할 때 표시 | 확정 |
| D-004 | 수집 게시판 | 지정된 6개 게시판과 질문과 답변 게시판(2300) | 확정 |
| D-005 | 임베딩 대상 | 광고·도배·삭제 콘텐츠를 제외한 모든 글을 임베딩하고 품질 신호로 검색 순위 조정 | 확정 |
| D-006 | 댓글 처리 | 삭제되지 않은 댓글·대댓글 전체를 수집하고 대화 구조 보존 | 확정 |
| D-007 | 미디어 처리 | 첨부 이미지는 OCR하고 영상·외부 링크는 제목과 URL만 보존 | 확정 |
| D-008 | 최초 수집 순서 | 최신 자료로 서비스를 먼저 열고 과거 최초 글까지 백그라운드 역순 수집 | 확정 |
| D-009 | 증분 동기화 | 하루 한 번 신규·수정·삭제 자료를 확인 | 확정 |
| D-010 | 출처 UX | 답변 아래 `출처 보기` 버튼으로 요청자에게 출처 목록 표시 | 확정 |
| D-011 | 상충 자료 | 최신 자료를 우선하되 상충 사실과 기준 날짜를 함께 고지 | 확정 |
| D-012 | LLM 장애 | 생성 대신 관련 게시물 제목·요약 일부·링크를 제공 | 확정 |
| D-013 | 개인정보 | 닉네임은 비가역 해시만 저장하고 전화번호·이메일·Discord 식별자 등은 저장·임베딩 전에 마스킹 | 확정 |
| D-014 | 관리 권한 | `DISCORD_OWNER_ID`로 지정한 봇 소유자만 관리 명령 사용 | 확정 |
| D-015 | 품질 검증 | 최소 100개 한국어 정답 세트로 검색·근거성·정확성 기준을 통과한 뒤 공개 | 확정 |
| D-016 | 1차 비목표 | 공개·멀티서버, 자동 대화 개입, 클라우드 LLM 폴백, 영상·음성 전사, 인벤 쓰기 동작 | 확정 |
| D-017 | 자율 결정 경계 | 로컬 라이브러리·DB·모델·포트·청킹·검색 튜닝은 OMX가 결정하며 새 데이터 소스와 D-016 항목의 범위 편입은 별도 승인 | 확정 |

## 3. 현재 확인된 실행 환경

- 저장소 루트: `/home/hyuns/project/Maple_Chat`
- Discord 토큰: 로그인 셸 환경의 `DISCORD_TOKEN`에 설정되어 있음. 토큰 값은 문서·로그·저장소에 기록하지 않는다.
- 참조 LLM 프로젝트: `/home/hyuns/local-claude-code`
- 참조 구성의 LLM 인터페이스: OpenAI 호환 vLLM API, 모델 별칭 `local-coder`, 기본 바인딩 `127.0.0.1:8000`
- 현재 호스트의 `127.0.0.1:8000`은 다른 프로젝트 API가 사용 중이므로, 실제 구현 시 LLM 포트 또는 Maple Chat의 접속 URL을 환경변수로 분리해야 한다.
- 호스트 아키텍처/GPU: Linux aarch64, NVIDIA GB10

## 4. 수집 대상(사용자 지정)

| 게시판 ID | 게시판 | 하위 분류(2026-08-04 관찰값) |
|---:|---|---|
| 2304 | 팁과 노하우 | 사냥, 전문기술, 아이템, 퀘스트, 몬스터, 지역, 테섭, 실험, 스토리, 메이플M, 기타 |
| 2294 | 전사 | 팁/정보, 히어로, 팔라딘, 다크나이트, 소울마스터, 아란, 데몬슬레이어, 미하일, 카이저, 데몬어벤져, 제로, 블래스터, 아델, 렌, 핑크빈 |
| 2295 | 마법사 | 팁/정보, 아크(불독), 아크(썬콜), 비숍, 플레임위자드, 에반, 배틀메이지, 루미너스, 키네시스, 일리움, 라라, 레테 |
| 2296 | 궁수 | 팁/정보, 보우마스터, 신궁, 윈드브레이커, 와일드헌터, 메르세데스, 패스파인더, 카인 |
| 2297 | 도적 | 팁/정보, 나이트로드, 섀도어, 나이트워커, 듀얼블레이드, 괴도팬텀, 카데나, 호영, 칼리 |
| 2298 | 해적 | 팁/정보, 메카닉, 바이퍼, 캡틴, 스트라이커, 캐논슈터, 엔젤릭버스터, 제논, 은월, 아크, 예티 |
| 2300 | 질문과 답변 | 목록 페이지에서 동적으로 발견·저장 |

하위 분류는 코드에 고정하지 않고 목록 페이지에서 발견한 값을 저장하여 신규 직업/분류에 대응한다.

## 5. 범위와 결정 경계

### 5.1 1차 출시 비목표

- 공개 초대와 여러 Discord 서버를 지원하는 멀티테넌트 봇
- 직접 멘션하지 않은 일반 채팅의 감시 또는 자동 답변
- 외부 클라우드 LLM API로 질문·검색 근거를 전송하는 폴백
- 영상 프레임 분석, 자막 추출, 음성 전사
- 인벤 로그인, 글쓰기, 댓글, 추천 등 사이트 상태를 변경하는 동작

추가 커뮤니티나 게시판 수집은 영구 비목표가 아닌 장래 확장 후보지만, 1차 출시 범위에는 포함하지 않는다. 대상 추가는 수집 권리·부하·파서·평가 세트 영향을 검토한 뒤 별도 승인해야 한다.

### 5.2 OMX 자율 결정 가능 범위

- 로컬 라이브러리와 패키지의 세부 버전
- DB 스키마의 내부 구현, 인덱스 파라미터, migration 분할
- 임베딩·재순위·OCR 모델의 로컬 실행 옵션
- 충돌하지 않는 loopback 포트, 컨테이너 리소스, worker 동시성
- 청크 크기·중첩, 검색 후보 수, RRF·품질 가중치, 점수 임계값
- 확정된 100개 이상 평가 세트와 비기능 기준을 통과하기 위한 안전한 튜닝

새 외부 데이터 소스, 외부 서비스로 데이터 전송, D-016의 기능 편입, 공개 배포, 유료 서비스 사용, 인벤 상태 변경은 사용자 승인 없이 추가할 수 없다.

## 6. 수집·색인 기본 원칙(확정된 결정에서 파생)

- 원문 레코드와 검색 청크를 분리한다. 원문에는 게시판/게시물/댓글 식별자, URL, 분류, 작성·수정 관찰 시점, 추천·조회·댓글 수와 부모 댓글 ID를 보존한다.
- 댓글 API가 제공하는 댓글 ID와 부모 ID를 사용하여 댓글·대댓글 트리를 복원한다. 본문과 무관한 짧은 반응도 원문에는 보관하되, 검색 시 정보량·추천·질문-답변 관계를 품질 신호로 사용한다.
- 본문과 댓글은 각각 검색 가능한 청크로 만들되, 댓글 청크에는 질문 본문 또는 부모 대화의 문맥을 덧붙여 단독 검색 시 의미가 사라지지 않게 한다.
- `팁/정보`, 인증·추천, 추천 수, 조회 수, 댓글 수, 최신성은 임베딩 포함 여부가 아니라 하이브리드 검색 및 재순위의 가중치로 사용한다.
- 동일 게시물의 수정은 안정적인 외부 키 `(board_id, article_id)`와 콘텐츠 해시로 감지하고 변경된 청크만 재임베딩한다.

## 7. 미디어 및 동기화 정책

- 본문에 첨부된 이미지는 원본 URL과 대체 텍스트를 보존하고 OCR 결과를 별도 필드로 저장한다. OCR 텍스트는 본문과 구분된 청크로 색인하며 낮은 OCR 신뢰도임을 검색·생성 단계에 전달한다.
- 영상 및 외부 링크는 페이지에 명시된 제목과 URL만 보존한다. 음성 전사, 영상 프레임 분석, 외부 페이지 본문 재귀 수집은 범위 밖이다.
- 초기 백필은 최신 페이지부터 시작한다. 최소 서비스 준비 기준을 충족하면 Discord 질의를 열고, 작업 큐가 게시판별 과거 페이지를 역순으로 계속 처리하여 접근 가능한 전체 이력을 완성한다.
- 증분 작업은 매일 한 번 실행한다. 신규 게시물·댓글 삽입, 콘텐츠 해시 변경에 따른 재색인, 삭제 감지에 따른 검색 제외를 한 주기로 처리한다.
- 삭제를 감지하면 원문과 벡터를 활성 검색에서 즉시 제외한다. 삭제된 본문은 보존하지 않고 `(board_id, article_id, deleted_observed_at)` tombstone만 남겨 재수집을 방지한다.

## 8. 외부 사이트 준수 및 출시 게이트

다음은 기술적 크롤링 가능 여부와 별개인 필수 출시 조건이다.

- 2026-08-04 확인한 `robots.txt`는 지정된 메이플 게시판 경로를 명시적으로 차단하지 않지만, 이는 콘텐츠 복제·임베딩·재제공에 대한 허가로 간주하지 않는다.
- 인벤 이용약관과 페이지 푸터에는 저작권, 게시물 권리, 사전 승낙 없는 복제·전송·배포 등에 관한 제한이 있으므로 **전체 이력 수집 전에 서비스 목적과 저장·요약·Discord 제공 방식에 대한 인벤의 허가 또는 적절한 법률 검토를 완료해야 한다.** 이 항목은 구현 완료가 아니라 운영 공개의 차단 게이트다.
- 이 게이트의 책임자는 봇을 배포하는 프로젝트 운영자다. 충족 증거는 인벤의 서면 허가 또는 자격 있는 검토자의 범위·조건·승인일이 포함된 기록이며, 증거가 없으면 실제 사이트 전체 백필과 Discord 공개를 시작하지 않는다. fixture와 승인된 소수 표본을 이용한 개발·테스트만 허용한다.
- 크롤러는 식별 가능한 User-Agent와 운영자 연락처를 사용하고, 동시 요청 수를 작게 제한하며, 요청 사이 지연·지수 백오프·`Retry-After`를 준수한다. `403`/`429`가 반복되면 자동 중단하고 우회 수단을 사용하지 않는다.
- 로그인, CAPTCHA 우회, IP 회전, 숨겨진/비공개 콘텐츠 접근은 금지한다. 공개 상태에서 정상 브라우저가 접근 가능한 본문·댓글·첨부 이미지만 대상으로 한다.
- Discord 답변에는 원문을 장문 재현하지 않고 질문에 필요한 최소한의 요약만 제공한다. 출처 요청 시에도 제목, 날짜, URL 중심으로 제공한다.
- 삭제·권리침해 신고를 받은 URL은 즉시 denylist에 넣어 크롤링·검색·생성에서 제외할 수 있어야 한다.

## 9. 확인된 크롤링 인터페이스 메모

- 목록 페이지에서 게시판 하위 분류와 게시물 URL을 발견할 수 있다. 분류 목록은 변경 가능한 원격 데이터로 취급한다.
- 게시물 본문은 HTML의 `#powerbbsContent` 영역에서 분리 가능하며 이미지·영상 URL이 함께 포함될 수 있다.
- 댓글은 페이지가 사용하는 공개 JSON 엔드포인트에서 별도로 로드된다. 응답의 댓글 ID와 부모 댓글 ID를 통해 대댓글 관계를 복원할 수 있다.
- 페이지 또는 댓글 응답 구조는 외부 계약이 아니므로 HTML fixture 기반 파서 회귀 테스트와 `parser_version`, 실패율 경보가 필요하다.

## 10. Discord 답변 및 출처 UX

- 허용된 Discord 서버의 허용 채널에서 `@봇 질문` 형태로 요청한다.
- 답변은 한국어로 작성하고 Discord 메시지 한도를 넘으면 의미 단위로 나누거나 embed를 사용한다. 봇은 원문을 그대로 길게 인용하지 않는다.
- 각 정상 답변 아래에 `출처 보기` 버튼을 제공한다. 클릭 시 해당 답변 생성에 실제 사용된 근거만 제목, 게시판/직업 분류, 작성일, URL 순으로 보여준다.
- 출처 응답은 버튼을 누른 사용자에게만 보이는 ephemeral 응답을 기본으로 한다. Discord 제한으로 ephemeral 응답이 불가능한 컨텍스트에서는 짧은 공개 답글로 대체한다.
- 답변과 출처의 연결은 TTL이 있는 `answer_id`로 보관하며, 서버 재시작 후에도 버튼이 유효한 기간 동안 실제 검색 근거를 재현할 수 있어야 한다.
- 최신성과 신뢰도가 충분한 근거가 없으면 추측하지 않고 “확인할 수 있는 근거가 부족하다”고 답한다.
- 서로 다른 패치 시점의 자료가 충돌하면 최신 자료에 가중치를 두되, 단정하지 않고 충돌 사실과 각 자료의 날짜를 표시한다. 테스트 서버 정보는 본 서버 정보와 별도 태그로 구분한다.
- 로컬 LLM 호출이 실패하거나 시간 초과되면 같은 검색 결과 중 상위 자료의 제목, 짧은 추출문, 날짜, 링크를 제공한다. 이 응답은 “AI 답변이 아닌 검색 결과”임을 명시한다.

## 11. 기술 아키텍처

### 11.1 구성 요소

1. **Discord bot/API**: 멘션 수신, 권한 검사, 질문 처리, 출처 버튼 및 관리자 명령
2. **Scheduler**: 일일 증분 동기화와 초기 전체 백필 작업 생성
3. **Crawler worker**: 목록·본문·댓글·이미지 수집, 파싱, 개인정보 정제, 콘텐츠 해시 계산
4. **Index worker**: 구조 기반 청킹, OCR, 임베딩, 벡터/검색 인덱스 갱신
5. **PostgreSQL + pgvector**: 원문 메타데이터, 작업 상태, 벡터, 답변-근거 연결, tombstone의 단일 영속 저장소
6. **Local model gateway**: `/home/hyuns/local-claude-code`가 제공하는 OpenAI 호환 vLLM 채팅 API

단일 서버·단일 호스트라는 확정 범위에서는 별도 Kafka/Milvus/Elasticsearch를 도입하지 않는다. 작업 큐는 PostgreSQL의 `FOR UPDATE SKIP LOCKED`, 재시도 횟수, lease 만료 시각을 이용해 내구성 있게 구현한다.

### 11.2 확정 스택

- Python 3.12, 비동기 `httpx`, `discord.py` 2.x, SQLAlchemy 2/Alembic
- PostgreSQL 16 + pgvector 0.8.x(HNSW cosine) + `pg_trgm` 기반 문자열 검색
- 한국어 포함 다국어 임베딩: `BAAI/bge-m3`, 1024차원
- 재순위: `BAAI/bge-reranker-v2-m3`
- OCR: CPU에서 동작하는 Tesseract 5 `kor+eng`; 교체 가능한 `OCRProvider` 인터페이스로 격리
- 배포: Docker Compose와 내부 전용 네트워크. 데이터베이스는 기본적으로 호스트 포트를 공개하지 않는다.

모델 이름, 차원, 거리 함수, 정규화 여부와 청킹 버전을 `embedding_revision`으로 저장한다. 임베딩 모델 변경 시 기존 벡터를 조용히 혼합하지 않고 새 revision을 병렬 생성한 뒤 활성 revision을 원자적으로 전환한다.

### 11.3 검색 파이프라인 계약

1. 질문에서 직업·게시판·본섭/테섭·기준 시점을 추출하되, 확신이 낮으면 강제 필터 대신 점수 가중치로 사용한다.
2. BGE-M3 dense cosine 상위 50건과 `pg_trgm` 제목/본문 검색 상위 50건을 병렬 조회한다.
3. Reciprocal Rank Fusion으로 결합하고, 최신성·팁/정보·인증·추천·질문-답변 구조의 품질 신호를 제한적으로 가산한다. 인기 점수만으로 의미 관련성을 뒤집지 못하게 상한을 둔다.
4. 상위 30건을 cross-encoder로 재순위한다.
5. 중복 청크와 동일 스레드의 반복 댓글을 제거하고, 서로 다른 관점과 최신 자료를 포함한 최대 8개 근거를 생성 컨텍스트로 선택한다.
6. 검색 점수가 기준보다 낮으면 생성하지 않고 근거 부족으로 응답한다.

### 11.4 초기 청킹 계약

- 제목, 게시판, 직업/분류, 작성일을 모든 청크의 검색용 머리말에 포함한다.
- 본문은 HTML 제목·문단·목록·표 경계를 우선하여 약 400~700 토큰, 최대 80 토큰 중첩으로 나눈다.
- 댓글은 최상위 댓글별 대화 가지를 하나의 Q&A 단위로 만들고 질문 본문의 짧은 문맥을 앞에 붙인다. 지나치게 긴 가지는 댓글 경계를 보존한 채 분할한다.
- OCR 결과는 이미지별 별도 청크로 만들고 원본 이미지 URL, 위치, OCR 신뢰도, 연결된 본문 청크 ID를 저장한다.
- 광고 UI, 메뉴, 서명, 레벨 아이콘 텍스트, 반복 인용, 빈 반응 등은 파싱/검색 단계에서 제거한다.

### 11.5 신뢰 경계

- 인벤 본문·댓글·OCR·외부 링크 제목은 모두 신뢰할 수 없는 데이터다. 그 안의 “이전 지시 무시”, 토큰 요청, 도구 호출 등의 문장을 시스템 지시로 실행하지 않는다.
- 수집 콘텐츠는 LLM 프롬프트에서 명확한 데이터 구획으로 감싸고, 콘텐츠에 포함된 URL을 봇이 자동 요청하거나 명령을 실행하지 않는다.
- Discord 입력은 길이, 멘션 수, URL 스킴을 제한하고 출력에서 `@everyone`, `@here`, 역할 멘션을 비활성화한다.
- `DISCORD_TOKEN`과 향후 비밀값은 환경변수/secret으로만 주입하고 예외·구조화 로그·관리자 상태 응답에서 마스킹한다.

### 11.6 로컬 LLM 운영 계약

- Maple Chat은 `/home/hyuns/local-claude-code`의 vLLM을 외부 로컬 의존 서비스로 취급하며 그 프로세스 수명주기를 직접 시작·중단하지 않는다.
- 구현·배포 기본 포트는 현재 8000번 충돌을 피하기 위해 loopback `8100`으로 정한다. 실제 주소는 `LLM_BASE_URL`로만 참조하여 포트 변경 시 코드 수정이 없게 한다.
- served model 이름은 참조 구성의 `local-coder`를 사용한다. 시작 시 `/v1/models`에서 해당 이름을 확인하고, 준비되지 않았으면 Discord는 검색 전용 폴백 모드로 동작한다.
- 생성 호출은 OpenAI 호환 `/v1/chat/completions`를 사용한다. 검색 컨텍스트는 기본 최대 12,000 토큰, 답변은 기본 최대 1,500 토큰으로 제한하며 평가 결과에 따라 로컬 튜닝할 수 있다.
- 임베딩과 재순위는 초기에는 CPU worker에서 실행하여 122B vLLM의 GPU/통합 메모리 SLA를 침범하지 않는다. GPU 사용은 동시 부하 시험에서 답변 목표를 유지한다는 증거가 있을 때만 허용한다.
- 클라우드 엔드포인트나 외부 API 키는 구성하지 않는다. 로컬 LLM 장애 시 FR-009의 검색 전용 폴백만 사용한다.

## 12. 기능 요구사항

### FR-001 Discord 질의 수신

- 봇은 설정된 단일 guild 및 허용 채널에서 자신을 직접 멘션한 메시지만 질문으로 처리한다.
- 봇 메시지, webhook 메시지, 다른 guild, 차단 사용자, 빈 질문은 처리하지 않는다.
- 원 질문의 작성자·채널·메시지 ID를 응답과 연결하되 Discord 메시지 본문은 기본적으로 영구 보관하지 않는다.
- 동시에 처리 가능한 생성 요청을 넘으면 순번을 알리고 대기열에 넣는다. 같은 메시지 ID의 중복 이벤트는 한 번만 처리한다.

### FR-002 근거 검색 및 답변 생성

- 질문마다 dense·lexical 후보 검색, fusion, 재순위, 중복 제거를 수행한다.
- 생성 모델에는 실제 선택된 근거와 메타데이터만 전달한다. 모델 자체 기억과 검색 근거가 충돌할 때 검색 근거를 우선하고, 근거가 없으면 단정하지 않는다.
- 답변은 본섭/테섭, 직업, 작성 시점, 패치 시점을 혼동하지 않아야 한다.
- 사용자가 여러 직업 또는 여러 질문을 한 번에 물으면 항목별로 답하고 각 항목의 근거를 별도로 연결한다.

### FR-003 출처 공개

- 모든 생성 답변에 `출처 보기` 버튼을 붙인다.
- 버튼은 해당 답변 생성 당시 실제 사용한 `answer_sources`만 표시하며 새 검색으로 바꿔치기하지 않는다.
- 출처에는 제목, 게시판/직업 분류, 작성일, 원문 URL을 제공한다. 원문이 삭제된 경우 삭제됨을 표시하고 링크를 비활성화한다.

### FR-004 게시판 발견 및 전체 백필

- 대상 게시판은 2304, 2294, 2295, 2296, 2297, 2298, 2300으로 초기화한다.
- 목록 페이지의 접근 가능한 페이지를 최신부터 과거 방향으로 순회하고, 게시판별 체크포인트를 저장한다.
- 작업 중단·배포·호스트 재부팅 후 마지막 성공 체크포인트부터 재개한다.
- 공지/고정/추천 영역에서 중복 노출되는 글은 `(board_id, article_id)`로 한 번만 저장한다.
- 게시판마다 최초 접근 가능한 일반 게시물까지 도달해야 해당 게시판 백필을 완료로 표시한다.

### FR-005 본문·댓글·미디어 수집

- 목록 메타데이터와 게시물 제목, 분류, 본문, 작성/수정 관찰 시각, 조회/추천/댓글 수, URL을 파싱한다.
- 공개 댓글 JSON에서 댓글·대댓글, 작성 시각, 추천 신호, 삭제 상태와 부모 관계를 수집한다.
- 본문 첨부 이미지의 원본 URL을 작업 큐에 넣고 허용 MIME, 크기 제한, 픽셀 제한을 통과한 이미지에만 OCR을 수행한다.
- 영상과 외부 링크는 본문에 나타난 제목/URL만 보존하며 대상 사이트를 재귀 크롤링하지 않는다.
- 파서 필수 필드가 사라지거나 정상 페이지의 파싱 성공률이 임계치 아래로 떨어지면 해당 게시판 작업을 실패 처리하고 알림을 발생시킨다. 빈 본문으로 성공 처리하지 않는다.

### FR-006 일일 증분 동기화

- Asia/Seoul 기준 설정된 시각에 7개 게시판의 신규 글과 신규 댓글을 확인한다.
- 최근 글 및 변경 가능성이 있는 글을 다시 요청하고 콘텐츠 해시·메타데이터 차이로 수정 여부를 판정한다.
- 원문 `404`, 삭제 표시 또는 목록/본문의 삭제 상태를 감지하면 활성 검색에서 제거하고 tombstone을 기록한다.
- 일일 작업이 겹치면 새로운 동일 작업을 만들지 않고 기존 run을 계속한다.

### FR-007 정제·청킹·색인

- 광고·도배·삭제 콘텐츠는 원문 상태와 제외 사유를 남기고 활성 색인에는 포함하지 않는다.
- 본문과 댓글에서 전화번호·이메일·Discord 식별자 등 민감정보를 마스킹하고 닉네임은 서비스별 salt를 사용한 비가역 해시로 바꾼 뒤 저장·청킹한다. 정제 전 콘텐츠는 영구 저장하지 않는다.
- 콘텐츠 또는 청킹 규칙이 바뀌면 영향받은 청크만 재생성한다.
- 같은 `(chunk_id, embedding_revision)` 색인은 멱등적이어야 하며 부분 실패 후 재실행해도 중복 벡터가 생기지 않는다.

### FR-008 상충·시간 정보 처리

- 검색 결과를 본섭, 테스트 서버, 이벤트/시즌 한정, 작성 시점으로 구분한다.
- 최신 자료에 우선순위를 주되 최신성만으로 낮은 관련도 자료가 상위에 오르지 않게 한다.
- 유사한 주장에 상반된 근거가 함께 검색되면 답변에 불확실성과 각 기준 날짜를 명시한다.

### FR-009 LLM 장애 폴백

- 연결 실패, 비정상 상태, 호출 시간 초과, 5xx, 응답 파싱 실패 시 한정 횟수만 재시도한다.
- 재시도 후에도 실패하면 상위 검색 결과의 제목, 날짜, 짧은 안전한 추출문, 링크를 제공한다.
- 폴백은 생성 답변이 아니며 원문 정보의 정확성을 보증하지 않는다는 문구를 표시한다.

### FR-010 운영·관리

- `DISCORD_OWNER_ID`와 일치하는 사용자만 관리 명령을 실행할 수 있으며 모든 관리 작업은 행위자와 결과를 감사 로그에 남긴다.
- 최소 관리 기능은 수집/백필/색인/LLM 상태, 게시판별 진행률, 마지막 성공 동기화, 실패 작업 수, 수동 동기화, 재색인, URL denylist, 채널 설정이다.
- 파괴적 작업은 대상과 예상 영향 확인 절차를 거치고 감사 로그를 남긴다.

## 13. 데이터 모델

| 엔터티 | 핵심 키/필드 | 목적 |
|---|---|---|
| `boards` | `board_id`, name, enabled, crawl_checkpoint | 대상 게시판과 백필 진행 상태 |
| `categories` | `board_id`, remote_name, first/last_seen_at | 동적으로 발견한 직업·분류 |
| `articles` | `board_id`, `article_id`, category, title, author_hash, sanitized_body, timestamps, counters, status, content_hash | 게시물 원문과 변경 상태 |
| `comments` | `board_id`, `article_id`, `comment_id`, `parent_comment_id`, author_hash, sanitized_body, timestamps, score, status, content_hash | 댓글 트리와 변경 상태 |
| `media_assets` | source URL hash, article key, MIME, dimensions, fetch status, OCR text/confidence | 첨부 이미지와 OCR 결과 |
| `chunks` | `chunk_id`, source type/key, ordinal, text, token_count, metadata, `chunking_revision`, active | 검색 가능한 불변 단위 |
| `embedding_revisions` | name, model, model commit, dimension, normalization, distance, activated_at | 벡터 호환성 계약 |
| `chunk_embeddings` | `chunk_id`, revision, vector, active | HNSW 검색 대상 |
| `crawl_runs` | run type, scope, state, counters, start/end/error | 백필·증분 실행 이력 |
| `crawl_jobs` | type, payload, state, priority, attempts, lease, next_attempt_at | 내구성 작업 큐 |
| `tombstones` | source key, deleted_observed_at, reason | 삭제 콘텐츠 재수집 방지 |
| `answers` | `answer_id`, Discord message refs, query hash, mode, timestamps, expiry | 답변과 출처 버튼 연결 |
| `answer_sources` | `answer_id`, `chunk_id`, retrieval/rerank scores, rank | 답변 당시 실제 근거 |
| `guild_settings` | guild ID, allowed channel IDs, owner/role policy | Discord 접근 범위 |
| `denylist` | normalized URL/source key, reason, actor, timestamps | 권리침해·운영 제외 |
| `feedback` | answer ID, rating/reason, actor hash, timestamp | 품질 개선 자료 |

모든 시각은 DB에 UTC로 저장하고 사용자 표시와 스케줄 계산만 `Asia/Seoul`을 사용한다. 외부 ID는 문자열 변환에 의존하지 않고 원래 정수 범위를 보존한다.

## 14. 상태 전이와 멱등성

- 수집 소스 상태: `discovered -> fetched -> parsed -> sanitized -> chunked -> indexed`, 예외 시 `retryable_failed | terminal_failed | deleted | denied`.
- 작업 상태: `queued -> leased -> succeeded`; lease 만료나 재시도 가능 오류는 지수 백오프 후 `queued`, 최대 횟수 초과 또는 구조 불일치는 `dead_letter`.
- 네트워크 타임아웃·429·5xx는 재시도 가능, 명시적 404/삭제는 tombstone, 반복 403/CAPTCHA는 전체 크롤러 회로 차단 대상으로 분류한다.
- DB upsert와 콘텐츠 해시를 사용하여 at-least-once 작업 실행에서도 결과는 exactly-once와 동등하게 유지한다.

## 15. 설정 계약

| 환경변수 | 필수 | 의미 |
|---|---:|---|
| `DISCORD_TOKEN` | 예 | 기존 로그인 셸에서 주입하는 Discord 봇 토큰 |
| `DISCORD_GUILD_ID` | 예 | 허용할 단일 guild |
| `DISCORD_OWNER_ID` | 예 | 관리 명령을 사용할 단일 봇 소유자 |
| `DATABASE_URL` | 예 | PostgreSQL 접속 문자열 |
| `LLM_BASE_URL` | 예 | OpenAI 호환 vLLM 주소; 배포 기본값 `http://127.0.0.1:8100/v1` |
| `LLM_MODEL` | 예 | 참조 구성 기준 `local-coder` |
| `LLM_TIMEOUT_SECONDS` | 아니오 | 생성 호출 제한 시간 |
| `EMBEDDING_MODEL` | 아니오 | 기본값 `BAAI/bge-m3` |
| `RERANKER_MODEL` | 아니오 | 기본값 `BAAI/bge-reranker-v2-m3` |
| `CRAWL_DAILY_AT` | 아니오 | KST 일일 동기화 시각 |
| `CRAWLER_USER_AGENT` | 예 | 서비스 식별 문자열과 버전 |
| `CRAWLER_CONTACT` | 예 | 사이트 운영자가 연락할 주소 |
| `PII_HASH_SALT` | 예 | 닉네임을 비가역 해시할 때 사용하는 secret |
| `LOG_LEVEL` | 아니오 | 구조화 로그 레벨 |

애플리케이션 시작 시 필수 설정을 검증하되 비밀값 자체는 오류 메시지에 포함하지 않는다. `.env.example`에는 예시 키만 두고 실제 토큰은 넣지 않는다.

## 16. 비기능 요구사항

### 16.1 성능 및 용량

- Discord 이벤트 수신 후 2초 이내에 처리 시작 신호(typing 또는 대기 안내)를 보낸다.
- 캐시가 준비된 정상 상태에서 검색·재순위 구간 p95 목표는 3초 이하다.
- 준비된 로컬 LLM에서 대기열 시간을 제외한 전체 답변 완료 p95는 60초 이하를 출시 목표로 한다. 모델 시작·가중치 적재 시간은 별도 운영 지표로 측정한다.
- 동일 시점 생성 요청은 기본 1개만 vLLM으로 보내고 나머지는 제한 길이의 내구성 대기열에서 처리한다. Discord 사용자별·채널별 속도 제한을 둔다.
- 스키마는 최소 30만 게시물, 300만 댓글, 500만 활성 청크까지 구조 변경 없이 처리할 수 있어야 한다. 실제 초기 백필 전에 표본 페이지로 크기·OCR 비율·평균 청크 수를 다시 산정한다.
- 원본 첨부 이미지 바이너리는 영구 저장하지 않고 OCR 성공/실패 후 캐시 TTL에 따라 제거하여 디스크 증가를 제한한다.

### 16.2 신뢰성

- 백필·일일 동기화·재색인은 프로세스 재시작 후 자동 재개되어야 한다.
- 게시판 하나의 오류가 다른 게시판 동기화를 막지 않으며, run 요약에는 부분 성공과 실패 범위가 분명히 표시되어야 한다.
- 일일 동기화는 다음 예정 실행 전까지 완료되는 것을 정상 기준으로 한다. 미완료 시 중복 run을 만들지 않고 관리자에게 경고한다.
- 외부 요청, DB 트랜잭션, OCR, 임베딩, LLM 호출에는 각각 별도 타임아웃과 제한된 재시도를 둔다.
- 애플리케이션 종료 시 새 lease를 중단하고 진행 중 트랜잭션을 완료하거나 안전하게 롤백한 뒤 종료한다.

### 16.3 관측 가능성

- JSON 구조화 로그에 `request_id`, `answer_id`, `crawl_run_id`, `job_id`, `board_id`, 단계, 소요 시간, 결과 코드를 기록한다.
- 원 질문 전문, 원문 본문, 댓글, OCR 텍스트, 닉네임, 토큰, DB URL은 기본 로그에서 제외한다.
- 최소 지표: Discord 요청/성공/오류/대기 시간, 검색·rerank·LLM 지연, 근거 부족률, LLM 폴백률, 게시판별 수집 성공률, 403/429/5xx, parser 실패율, 작업 큐 깊이, 백필 진행률, 임베딩 처리량.
- 상태 점검은 bot, DB, embedding/reranker, vLLM, scheduler freshness를 분리해서 보여주며 하나의 종합 `ready` 판정도 제공한다.

### 16.4 보안 및 개인정보

- Discord·관리 명령은 guild/channel/사용자 또는 역할 allowlist를 서버 측에서 검증한다. UI에서 명령이 보이지 않는 것만으로 권한을 대체하지 않는다.
- DB 컨테이너와 내부 API는 외부 인터페이스에 바인딩하지 않는다. vLLM도 loopback 또는 전용 내부 네트워크에서만 접근한다.
- 컨테이너는 가능한 비루트 사용자, 읽기 전용 루트 파일시스템, 필요한 디렉터리만 쓰기 볼륨, 최소 Linux capability로 실행한다.
- 의존성 버전과 모델 revision을 고정하고 라이선스·취약점 검사를 CI에 둔다.
- 권리침해 URL 차단과 삭제 처리는 캐시, 원문, 청크, 벡터, 답변 출처에 일관되게 전파된다.

### 16.5 백업·복구

- PostgreSQL은 매일 백업하고 최근 7개 일간 백업과 최근 4개 주간 백업을 보존하는 것을 기본안으로 한다.
- 원문/메타데이터와 운영 설정은 백업 대상이며, 재생성 가능한 벡터는 복구 시간과 저장 공간을 고려하여 선택적으로 백업한다.
- 복구 절차는 빈 DB에서 migration, 백업 복원, 활성 embedding revision 검증, 미완료 작업 lease 정리, Discord 재접속 순서로 문서화한다.
- 출시 전 최소 한 번 별도 임시 DB로 복원 훈련을 수행한다.

## 17. 테스트 전략

### 17.1 자동화 테스트

- **단위 테스트**: URL 정규화, 분류 발견, HTML 정제, 개인정보 마스킹, 콘텐츠 해시, 댓글 트리, 청킹, 점수 결합, 시간/테섭 태그, 멘션 제거
- **파서 fixture 테스트**: 7개 게시판 목록, 일반/공지/삭제/이미지/영상/장문 게시물, 댓글 없음/대댓글/삭제 댓글/베스트 댓글 JSON을 개인정보가 제거된 fixture로 고정
- **계약 테스트**: 실제 사이트에는 낮은 빈도의 소수 읽기 요청만 보내 필수 selector와 댓글 응답 스키마가 살아 있는지 확인; 실패 시 크롤링 중단
- **DB 통합 테스트**: migration, unique key, upsert 멱등성, lease 경쟁, tombstone, revision 전환, HNSW/문자열 검색
- **RAG 테스트**: hybrid 후보 결합, 메타데이터 필터, 재순위, 낮은 점수 거절, 상충 날짜, 프롬프트 인젝션 문서, 출처 일치
- **Discord 테스트**: 허용/비허용 guild·channel, 멘션, 중복 이벤트, 긴 답변 분할, 출처 버튼, 만료 버튼, 관리자 권한, LLM 폴백
- **장애 주입**: 429/403/5xx/timeout, HTML 구조 변경, 손상 이미지, OCR 실패, embedding OOM, DB 재시작, vLLM 중단, worker 강제 종료

### 17.2 품질 평가

- 최소 100개 한국어 질문과 사람이 확인한 정답 URL/허용 답변/시점/직업 메타데이터를 버전 관리한다.
- 직업 5계열과 각 하위 직업, 일반 팁, 질문과 답변, 이미지 OCR, 테섭/본섭, 오래된 정보 충돌, 근거 없음이 모두 포함되어야 한다.
- 검색 출시 기준: 정답 근거 Recall@10 0.85 이상, MRR@10 0.70 이상. 직업별 최저 Recall@10도 별도 보고하여 전체 평균으로 취약 직업을 가리지 않는다.
- 답변 출시 기준: 사람 평가에서 근거 충실성 4/5 이상 90%, 핵심 정확성 4/5 이상 85%, 근거 없는 단정 2% 미만, 출처 버튼의 실제 근거 일치율 100%.
- 보안 세트에서 수집 문서의 프롬프트 인젝션을 따르거나 비밀값·시스템 지시를 노출하는 사례는 0건이어야 한다.
- 기준 미달 시 프롬프트만 임의 조정하지 않고 실패를 검색 누락, 재순위, 청킹, 최신성, 생성 오류로 분류해 수정한다.

## 18. 단계별 출시 계획

1. **Discovery gate**: 인벤 허가/법률 검토, Discord application 권한, 로컬 LLM 포트·상태, 디스크 여유 확인
2. **Vertical slice**: 게시판 1개·소수 글 fixture/허용 표본으로 수집→청킹→임베딩→검색→Discord 답변→출처 버튼의 전 구간 검증
3. **Seven-board recent sync**: 7개 게시판 최신 범위와 댓글/OCR를 처리하고 일일 증분 실행
4. **Private beta**: 지정한 단일 Discord 서버에서 근거 기반 답변과 관리자 기능 검증
5. **Full backfill**: 최신 우선 서비스와 병행하여 게시판별 최초 접근 가능 글까지 완료
6. **Quality gate**: 확정된 평가 세트·성능·보안·복구 기준 통과
7. **Operational handoff**: 운영 명령, 장애 대응, 차단·삭제, 모델 revision 전환, 백업 복구 문서 확정

## 19. 구현 완료 정의(Definition of Done)

- [ ] D-001~마지막 인터뷰 결정이 모두 확정되고 미결 항목이 없다.
- [ ] 7개 게시판과 동적 하위 직업/분류가 설정·DB·테스트에서 일치한다.
- [ ] 접근 가능한 전체 이력의 게시판별 백필 완료 증거(최초 글, 페이지 체크포인트, 성공/제외/실패 합계)가 있다.
- [ ] 광고·도배·삭제를 제외한 본문과 전체 댓글·대댓글이 정책대로 청크 및 활성 embedding revision에 반영됐다.
- [ ] 첨부 이미지 OCR과 영상/외부 링크 메타데이터 정책이 표본 및 자동 테스트로 검증됐다.
- [ ] 일일 신규·수정·삭제 동기화가 연속 3회 성공하고 수정 재색인·삭제 tombstone이 검증됐다.
- [ ] Discord 멘션 Q&A, 출처 버튼, 긴 응답, 근거 부족, 상충 정보, LLM 장애 폴백이 실제 테스트 guild에서 동작한다.
- [ ] 실제 LLM 호출이 `/home/hyuns/local-claude-code` 기반 OpenAI 호환 서비스와 확정 모델명을 사용한다.
- [ ] `DISCORD_TOKEN`은 환경에서만 주입되고 저장소·이미지·로그·Discord 응답 어디에도 유출되지 않는다.
- [ ] 확정된 검색/답변 품질, 성능, 프롬프트 인젝션, 권한, 복구 테스트를 통과한다.
- [ ] 인벤 콘텐츠 수집·요약·제공에 대한 운영 출시 게이트가 충족됐다.
- [ ] README, 운영 runbook, 데이터 삭제/권리침해 대응, migration/rollback 문서가 현재 구현과 일치한다.
