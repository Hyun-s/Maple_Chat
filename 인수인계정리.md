# Maple Chat 작업 인수인계

> **주의(2026-08-25):** `현재 상태` 아래의 내용이 최신이며, 그 다음 내용은 2026-08-21 시점의
> 과거 인수인계 기록이다. 현재 구현·DB·운영
> 상태는 `docs/current-system-overview.md`, 직업 지식 그래프의 이론과 구현은
> `docs/knowledge-graph.md`, 품질 실패와 해결 이력은 `docs/service-quality-log.md`를 기준으로 한다.

## 현재 상태 — 2026-08-25

- 공식 보스 가이드 기준 상시 보스 33개, 실제 난이도 변형 79개를 지식 그래프에 등록했다.
- 시즌 보스 메이린은 별도 공식 update provenance로 유지한다.
- 데스티니는 무기와 해방 퀘스트 보스 모드의 다의어로 모델링했다. 데스티니 모드의 세렌·칼로스·
  카링 boss variant와 각 특수 조건은 공식 1.2.401 provenance를 가진다.
- 내장 graph 동기화 규모: source 7개, entity 217개, alias 755개, relation 292개.
- 로컬 Qwen 호출은 usage 포함 SSE streaming이며, 생성 답변 하단에 입력/출력/총 token, 입력·출력
  TPS, QA 전체 시간을 표시한다. 동일 값은 원문 없이 `discord_answer_completed` JSON 로그에 남는다.
- 입력 TPS는 prompt token을 TTFT로 나눈 유효 prefill 속도이며 queue/HTTP 시간을 포함한다.
- 과거 Inven 백필 목표를 게시판별 72페이지에서 200페이지로 확대했다. 10페이지 round-robin과 단일 crawl lock은 유지한다.
- 200페이지 백필은 tmux `maple-backfill` 창에서 PID `1106880`으로 실행 중이며 `logs/full-backfill-200.log`에 누적한다. 첫 작업은 팁과 노하우 36~40페이지다.
- 최근 365일 넥슨 공식 패치노트 30건을 운영 DB에 저장했고 555개 vector를 색인했다.
- 재실행 결과 상세 fetch 0건, unchanged 30건으로 증분 멱등성을 확인했다.
- `scripts/weekly_patch_notes_loop.sh`가 매주 목요일 14:00 KST에
  `scripts/sync_patch_notes.sh`를 별도 lock으로 실행한다.
- 현재 PID: bot `999779`, 일일 Inven loop `999662`, 주간 패치노트 loop `999663`.
- 다음 패치노트 예약: 2026-08-27 14:00 KST.
- 즉시 수동 동기화: `scripts/sync_patch_notes.sh`
- 관련 품질 이력: `docs/service-quality-log.md`의 SQ-2026-08-25-009~011.
- 전체 검증: 193 tests, coverage 90.35%, Alembic 왕복, Ruff/format/Mypy 통과.

  작업 경로: `/home/hyuns/project/Maple_Chat`

  ## 즉시 목표

  추가 Inven 크롤링 없이 현재 PostgreSQL 데이터와 GPU 인덱스를 사용해 기존 Discord 애플리케이션 `ClaudeBot`을 실행하고 실제 멘

  션 응답을 검증한다.

  ## 현재 데이터

  - 게시글: 2,191건

  - 댓글: 7,647건

  - 활성 청크: 9,930건

  - 활성 임베딩: 9,930건

  - 임베딩 revision: `bge-m3-5617a9f61b02`

  - OCR: 사용하지 않음

  - 2294 백필: 55페이지 완료, 다음 체크포인트 56

  - 백필은 `RemoteProtocolError`로 중단됐지만 저장 데이터는 정상

  - 사용자는 추가 크롤링 없이 현재 데이터만 사용하길 원함

  ## 모델/GPU

  - Qwen compose:

    `/home/hyuns/local-claude-code/docker-compose.qwen38-27b.yml`

  - 컨테이너: `vllm-local-coder`

  - 모델: `Qwen/Qwen3.8-27B`

  - served name: `local-coder`

  - API: [`http://127.0.0.1:8001/v1`](http://127.0.0.1:8001/v1)

  - embedding: `BAAI/bge-m3`, CUDA

  - reranker: `BAAI/bge-reranker-v2-m3`, GPU

  - GPU: NVIDIA GB10

  실제 DB 검색 → GPU rerank → Qwen 답변 생성까지 성공했다.

  ## 적용된 수정

  - UTF-8/CP949 적응형 HTML 디코딩

  - `#powerbbsContent` 본문 경계 누출 수정

  - 삭제 글과 검색 텍스트 없는 이미지 글 안전 제외

  - OCR 비활성화

  - token 없는 Inven 댓글 API 지원

  - 댓글 수와 현재 댓글 JSON 스키마 처리

  - CUDA PyTorch와 `EMBEDDING_DEVICE=cuda`

  - Qwen timeout 180초

  - 기본 생성 상한 384 tokens

  - 답변을 8문장 이내로 제한

  ## 검증

  - 전체 unit tests: 122 passed

  - Ruff: 통과

  - mypy: 46개 소스 파일 통과

  - PostgreSQL hybrid retrieval: 성공

  - GPU BGE rerank: 성공

  - local Qwen grounded generation: 성공

  ## Discord 설정

  - 기존 애플리케이션: `ClaudeBot`

  - `DISCORD_TOKEN`은 `~/.bashrc`에 export되어 있음

  - 토큰을 출력하거나 파일에 기록하지 말 것

  - 사용자는 `.env`에 Discord ID 설정을 완료했다고 말함

  필수 프로젝트 설정:

  ```dotenv

  DISCORD_GUILD_ID=&lt;서버 ID&gt;

  DISCORD_OWNER_ID=&lt;운영자 본인 사용자 ID&gt;

  DISCORD_CHANNEL_IDS=&lt;허용 채널 ID&gt;

  현재 데이터만 사용하기 위한 설정:

  LIVE_CRAWL_ENABLED=false

  RUN_LIVE_SYNC_ON_START=false

  RUN_DAILY_SYNC=false

  LIVE_CRAWL_INCLUDE_OCR=false

  Discord 권한:

  - View Channels

  - Send Messages

  - Read Message History

  봇은 허용 채널에서 직접 @ClaudeBot 질문으로 멘션된 메시지만 처리한다.

  ## 다음 실행 순서

  1. 비밀값을 출력하지 않고 필수 환경변수의 set/missing 여부만 확인

  2. 크롤링 관련 플래그 네 개가 false인지 확인

  3. bot config 검증

  4. PostgreSQL과 Qwen readiness 확인

  5. scripts/run_live_[bot.sh](http://bot.sh) 장기 실행

  6. Discord Gateway 로그인 성공 확인

  7. 허용 채널에서 @ClaudeBot 질문을 보내 실제 RAG 답변과 출처 버튼 확인

  명령:

  cd /home/hyuns/project/Maple_Chat

  source ~/.bashrc

  bash -lc 'source scripts/_local_[env.sh](http://env.sh); conda_run maple-chat validate-config --role bot'

  scripts/run_live_[bot.sh](http://bot.sh)

  ## 중요

  이전 세션에서는 stale OMX Conductor 상태 때문에 모든 실행 및 파일 쓰기 명령이 PROVENANCE_DENIED로 차단됐다. Discord 봇은 아

  직 실제로 시작되지 않았고 Gateway 로그인도 검증하지 못했다.

  새 세션에서는 먼저 이 내용을 docs/[session-handoff-2026-08-21.md](http://session-handoff-2026-08-21.md)로 저장한 뒤 Discord 봇을 실행하라.

  .env, Discord token, DB password, PII salt를 출력하거나 커밋하지 말 것. 작업 트리에 기존 사용자 변경이 많으므로 reset/clean

  하지 말 것.
